const LoadingSpinner = () => (
    <div className="flex justify-center items-center py-10"> {/* Increased padding */}
        <span className="loading loading-spinner loading-lg text-primary"></span>
    </div>
);

export default LoadingSpinner;
